<Page
    x:Class="test.Views.DownloadsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:test.Models"
    mc:Ignorable="d">

    <Page.Resources>
        <Style x:Key="TransparentButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="8" />
        </Style>
    </Page.Resources>

    <Grid Margin="{StaticResource NavigationViewPageContentMargin}" Padding="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock
            Grid.Row="0"
            Margin="0,0,0,16"
            Style="{ThemeResource TitleTextBlockStyle}"
            Text="Downloads" />

        <ListView
            x:Name="DownloadsList"
            Grid.Row="1"
            IsItemClickEnabled="True"
            ItemClick="DownloadsList_ItemClick"
            ItemsSource="{x:Bind ViewModel.Downloads, Mode=OneWay}"
            SelectionMode="None">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    <Setter Property="Padding" Value="0" />
                    <Setter Property="Margin" Value="0,4" />
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:DownloadItem">
                    <Border
                        Padding="16"
                        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1"
                        CornerRadius="8">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <Border
                                Grid.Column="0"
                                Width="48"
                                Height="48"
                                Margin="0,0,12,0"
                                CornerRadius="8">
                                <Image Source="{x:Bind LogoUrl, Mode=OneWay}" Stretch="UniformToFill" />
                            </Border>

                            <StackPanel
                                Grid.Column="1"
                                VerticalAlignment="Center"
                                Spacing="4">
                                <TextBlock
                                    FontWeight="SemiBold"
                                    Text="{x:Bind Title, Mode=OneWay}"
                                    TextTrimming="CharacterEllipsis" />
                                <TextBlock
                                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                    Style="{ThemeResource CaptionTextBlockStyle}"
                                    Text="{x:Bind PublisherName, Mode=OneWay}" />
                                <TextBlock
                                    Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                                    Style="{ThemeResource CaptionTextBlockStyle}"
                                    Text="{Binding StatusText, Mode=OneWay}"
                                    Visibility="{Binding Status, Converter={StaticResource NotDownloadingStatusToVisibilityConverter}, Mode=OneWay}" />
                                
                                <!-- Progress section for active downloads -->
                                <Grid Visibility="{Binding Status, Converter={StaticResource DownloadStatusToVisibilityConverter}, Mode=OneWay}">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <ProgressBar
                                        Grid.Column="0"
                                        Width="200"
                                        HorizontalAlignment="Left"
                                        VerticalAlignment="Center"
                                        Maximum="100"
                                        Value="{Binding Progress, Mode=OneWay}" />
                                    <Button
                                        Grid.Column="1"
                                        Margin="8,0,0,0"
                                        Padding="4"
                                        VerticalAlignment="Center"
                                        Background="Transparent"
                                        BorderThickness="0"
                                        Click="CancelButton_Click"
                                        Tag="{x:Bind ProductId}"
                                        ToolTipService.ToolTip="Cancel download">
                                        <FontIcon FontSize="12" Glyph="&#xE711;" />
                                    </Button>
                                </Grid>
                            </StackPanel>

                            <StackPanel
                                Grid.Column="2"
                                VerticalAlignment="Center"
                                Orientation="Horizontal"
                                Spacing="8">
                                
                                <!-- Checkmark for completed -->
                                <FontIcon
                                    VerticalAlignment="Center"
                                    FontSize="20"
                                    Foreground="{ThemeResource SystemFillColorSuccessBrush}"
                                    Glyph="&#xE73E;"
                                    Visibility="{Binding Status, Converter={StaticResource CompletedStatusToVisibilityConverter}, Mode=OneWay}" />
                                
                                <!-- 3-dot menu for non-downloading states -->
                                <Button
                                    Padding="8"
                                    VerticalAlignment="Center"
                                    Background="Transparent"
                                    BorderThickness="0"
                                    Visibility="{Binding Status, Converter={StaticResource NotDownloadingStatusToVisibilityConverter}, Mode=OneWay}">
                                    <FontIcon FontSize="16" Glyph="&#xE712;" />
                                    <Button.Flyout>
                                        <MenuFlyout>
                                            <MenuFlyoutItem
                                                Click="DeleteMenuItem_Click"
                                                Tag="{x:Bind ProductId}"
                                                Text="Delete">
                                                <MenuFlyoutItem.Icon>
                                                    <FontIcon Glyph="&#xE74D;" />
                                                </MenuFlyoutItem.Icon>
                                            </MenuFlyoutItem>
                                        </MenuFlyout>
                                    </Button.Flyout>
                                </Button>
                            </StackPanel>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>